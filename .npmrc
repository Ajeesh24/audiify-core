# Use public npm registry for this project (overrides corporate settings)
registry=https://registry.npmjs.org/

# Disable package-lock for scoped packages that might conflict
# @abk:registry=https://registry.npmjs.org/

# Cache settings
cache=/tmp/.npm